#pragma once

#include <vector>
#include <string>
#include <glm/glm.hpp>

namespace Core::Paint {

	struct PaintLayer {
		std::string name = "New Layer";
		bool visible = false;
		float opacity = 1.0f;

		// Later 

	};
}